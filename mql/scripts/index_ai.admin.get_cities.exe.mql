script create exe "index_ai.admin.get_cities" false 1 
  (param
  	(var "[db_id]" {true} "The database id" is_null:false is_empty:false "db1")
  ;) 
  "Get cities" 
{

	json load "cmds" "[]";

	json load "cmd" "{}";
	json iobject "cmd" / t "CITIES" "STR";
	json iobject "cmd" / db [db_id] "STR";

	json iarray "cmds" / (json doc "cmd") OBJ;
	
	index_ai execute (json doc "cmds");
	
} "Return nothing";
script set delay "index_ai.admin.get_cities.exe" 0 day {1;};
